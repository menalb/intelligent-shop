export interface BuyProduct {
    kind: 'add-to-cart';
    product?: string;
}

export interface Product {
    name: string;
    imageUrl: string;
}

export interface Product {
    name: string;
    imageUrl: string;
    description: string;
}
